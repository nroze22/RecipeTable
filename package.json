{
  "name": "recipe-table",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "worker"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "check": "tsc -b --pretty false && npm run check --workspace worker",
    "test": "vitest run && npm run test --workspace worker",
    "test:watch": "vitest",
    "worker:dev": "npm run build && npm run dev --workspace worker",
    "worker:deploy": "npm run deploy --workspace worker",
    "deploy": "npm run build && npm run worker:deploy"
  },
  "dependencies": {
    "@tesseract.js-data/eng": "^1.0.0",
    "html-to-image": "^1.11.13",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.5",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vite-plugin-pwa": "^1.3.0",
    "vite-plugin-static-copy": "^4.1.1",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22"
  }
}
